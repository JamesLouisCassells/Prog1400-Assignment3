package GUI;

import javax.swing.*;
import java.awt.*;

public class CreationPanel extends JPanel {

    private GameFrame frame;

    public CreationPanel(GameFrame frame) {

        this.frame = frame;

        setBackground(new Color(140,155,220));
        setLayout(null);

    }
}